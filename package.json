{
  "name": "ast-highlight-helper",
  "displayName": "AST Highlight Helper",
  "description": "Show AST representation of highlighted code using Luau/Lute",
  "version": "0.0.1",
  "repository": {
    "type": "git",
    "url": "https://github.com/your-username/ASTHighlightHelper.git"
  },
  "engines": {
    "vscode": "^1.74.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:ast-highlight-helper.showAST"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "ast-highlight-helper.showAST",
        "title": "Show AST of Selected Code"
      }
    ],
    "keybindings": [
      {
        "command": "ast-highlight-helper.showAST",
        "key": "ctrl+shift+a",
        "mac": "cmd+shift+a",
        "when": "editorTextFocus && editorHasSelection"
      }
    ],
    "configuration": {
      "title": "AST Highlight Helper",
      "properties": {
        "astHighlightHelper.hotkey": {
          "type": "string",
          "default": "ctrl+shift+a",
          "description": "Hotkey to trigger AST display"
        },
        "astHighlightHelper.luteExecutable": {
          "type": "string",
          "default": "",
          "description": "Path to the Lute executable (leave empty for auto-detection)"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "npm run build:frontend && tsc -p ./",
    "build:frontend": "cd frontend && npm run build",
    "watch": "tsc -watch -p ./",
    "dev": "npm run build:frontend && npm run watch"
  },
  "devDependencies": {
    "@types/node": "16.x",
    "@types/react": "^19.1.8",
    "@types/vscode": "^1.74.0",
    "typescript": "^4.9.4"
  },
  "dependencies": {
    "child_process": "^1.0.2",
    "react": "^19.1.0"
  }
}
